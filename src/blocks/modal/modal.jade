include ../authorInfo/authorInfo

mixin modal
    +b('section').modal(id="author")
        +e('div').table
            +e('div').cell
                +e('div').content
                    +authorInfo

                +e('div').footer
                    +e('a').link(href="#", class="button modal_close")!= "Закрыть"
